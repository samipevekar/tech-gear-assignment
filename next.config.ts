import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com','www.soundguys.com','m.media-amazon.com'],
  },
};

export default nextConfig;
